{
    "server": {
        "userGRPCport": ":9101",
        "userservicehost": "http://127.0.0.1"
    },
    "logger": {
        "level": "debug",
        "filename": "./app.log"
    },
    "database":{
        "migration":"postgres://postgres:postgres@localhost:5432/user_service?sslmode=disable",
        "casbin": "postgres://postgres:postgres@localhost:5432/user_service?sslmode=disable",
        "dns": "user=postgres password=postgres dbname=user_service host=localhost port=5432",
        "maxconn": 30
    }
}